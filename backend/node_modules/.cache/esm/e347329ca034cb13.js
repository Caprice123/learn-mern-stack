let express,cors,mongoose,dotenv,usersRoute;_a03‍.w("express",[["default",["express"],function(v){express=v}]]);_a03‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_a03‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_a03‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_a03‍.w("./users",[["default",["usersRoute"],function(v){usersRoute=v}]]);





dotenv.config()

const app = express();
const port = process.env.PORT || 5000

app.use(cors())
app.use(express.json())

app.get('/', (req, res) => {
    res.send('GET request to homepage')
})

const uri = process.env.ATLAS_URI
mongoose.connect(uri, { useNewUrlParser: true })

const connection = mongoose.connection
connection.once('open', () => {
    console.log("MongoDB database connection established successfully")
})

// import exercisesRoute from 'route/exercises'


// app.use('/exercises', exercisesRoute)
// app.route('/users', usersRoute)


app.listen(port, () => {
    console.log(`Server is running  on port: ${port}`)
})